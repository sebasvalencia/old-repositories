import { DragNdropFileDirective } from './drag-ndrop-file.directive';

describe('DragNdropFileDirective', () => {
  it('should create an instance', () => {
    const directive = new DragNdropFileDirective();
    expect(directive).toBeTruthy();
  });
});
